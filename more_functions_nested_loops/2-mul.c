#include "main.h"
/**
 * mul - task 01
 * @a : variable 1th to multiplicate
 * @b : variable 2th to multiplicate
 * Description:  function that checks for a digit (0 through 9)
 * Return: always
 */
int mul(int a, int b)
{
	int mult = 0;

	mult = a * b;
	return (mult);
}
