#include "main.h"
/**
 * add - task 09
 * @a : numero 1
 * @b : numero 2
 * Description: unction that adds two integers and returns the result
 * Return: always
 */
int add(int a, int b)
{
	int suma;

	suma = a + b;
	return (suma);
}
