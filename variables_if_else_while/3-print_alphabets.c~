#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Alphabet in lowercase, and then in uppercase'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{char ch = 'A';
	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
